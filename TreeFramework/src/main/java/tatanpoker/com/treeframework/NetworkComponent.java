package tatanpoker.com.treeframework;

import java.awt.Frame;

public class NetworkComponent implements Component {
    @Override
    public Component getComponent() {
        if(Framework.getFramework() == )
    }
}
